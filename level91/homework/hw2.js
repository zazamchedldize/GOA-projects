// 2)let number = -7;

// გამოიტანე:

// თუ რიცხვი 0-ზე მეტია → "დადებითი"
// თუ 0-ზე ნაკლებია → "უარყოფითი"
// თუ 0-ის ტოლია → "ნულია"


let number = 7

if (number > 0) {
    console.log("დადებითი")
} else if (number < 0) {
    console.log("უარყოფითი")
} else {
    console.log("ნულია")
}